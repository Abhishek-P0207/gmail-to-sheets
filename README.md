# Gmail to Sheets

## Setup

1. Install dependencies:
   ```
   pip install -r requirements.txt
   ```

2. Add your credentials to `credentials/credentials.json`

3. Run the application:
   ```
   python src/main.py
   ```
